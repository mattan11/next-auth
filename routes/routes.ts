export const publicRoutes = ['/', '/new-verification'];

export const authRoutes = ['/sign-in', '/sign-up', '/error', '/reset'];

export const apiAuthPrefix = '/api/auth';

export const DEFAULT_LOGIN_REDIRECT = '/settings';
